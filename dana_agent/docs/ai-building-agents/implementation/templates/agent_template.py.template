"""
Agent Template

Copy this template to create a new agent.
Replace all [PLACEHOLDER] text with your specific implementation.
"""

from dana.core.agent.star_agent import STARAgent


class [AgentName]Agent(STARAgent):
    """
    <PUBLIC_DESCRIPTION>
    [Describe what this agent does - visible to other agents and users]

    Use this agent for:
    - [Use case 1]
    - [Use case 2]
    - [Use case 3]

    Capabilities:
    - [Capability 1]
    - [Capability 2]
    - [Capability 3]
    </PUBLIC_DESCRIPTION>

    <PRIVATE_IDENTITY>
    You are [agent role description].

    Your process:
    1. [Step 1]
    2. [Step 2]
    3. [Step 3]

    Your principles:
    - [Principle 1]
    - [Principle 2]
    - [Principle 3]

    You are [personality traits: methodical, thorough, etc.].
    </PRIVATE_IDENTITY>
    """

    def __init__(self, agent_id: str | None = None, **kwargs):
        """
        Initialize [AgentName]Agent.

        Args:
            agent_id: Agent identifier (default: "[agent-id]")
            **kwargs: Additional arguments for STARAgent
        """
        super().__init__(
            agent_type="[agent-type]",
            agent_id=agent_id or "[agent-id]",
            **kwargs
        )

        # Compose capabilities using fluent builder pattern
        self.with_workflows(
            # Add workflows this agent can execute
            # [Workflow1](workflow_id="workflow-1"),
            # [Workflow2](workflow_id="workflow-2"),
        ).with_resources(
            # Add resources this agent can use
            # [Resource1](resource_id="resource-1"),
            # [Resource2](resource_id="resource-2"),
        )

        # Optionally add sub-agents (for coordinator agents)
        # self.with_agents(
        #     [SubAgent1](),
        #     [SubAgent2](),
        # )


# Example usage:
# agent = [AgentName]Agent()
# result = agent.query(message="Hello, can you help me with...")
# print(result)
