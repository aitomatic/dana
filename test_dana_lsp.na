# Test Dana file for LSP functionality
# This file demonstrates various Dana language features

# Correct scope syntax
private:name = "Dana LSP Test"
public:version = "0.1.0"

# Function definition
def greet(name: str) -> str:
    private:message = f"Hello, {name}!"
    log.info(private:message)
    return private:message

# Conditional logic
if private:name == "Dana LSP Test":
    log.info("LSP test file loaded successfully")
    public:status = "ready"
else:
    log.warn("Unexpected test file name")

# Agent definition example
agent TestAgent:
    model: str = "gpt-4"
    temperature: float = 0.7

# Struct definition example  
struct TestData:
    id: int
    value: str
    timestamp: float = 0.0

# Use statement example
private:llm = use(
    provider="openai",
    model="gpt-4"
)

# Some intentional issues for LSP to catch:
# 1. Incorrect scope syntax (should trigger warning)
private.bad_scope = "This should use colon not dot"

# 2. String concatenation (should suggest f-string)
private:old_style = "Result: " + str(42)

# 3. Unscoped variable (should suggest scope prefix)
unscoped_var = "This should have a scope prefix"

log.info("Dana LSP test file complete") 