log_level("INFO")

log("=== DEBUG PROMISE CREATION ===")

# Test 1: Check what llm returns
log("Test 1: What does llm return?")
x = llm("hi")
log(f"x type: {type(x)}")
log(f"x: {x}")

# Test 2: Check if it's an EagerPromise
log("\nTest 2: Is it an EagerPromise?")
from dana.core.concurrency import is_eager_promise
log(f"is_eager_promise(x): {is_eager_promise(x)}")

# Test 3: Check if it's a coroutine
log("\nTest 3: Is it a coroutine?")
import inspect
log(f"inspect.iscoroutine(x): {inspect.iscoroutine(x)}")

log("=== END DEBUG ===") 