# Test script to verify TUI logging fix
# This should only log to the TUI log panel, not to console

log("Starting TUI logging test")

# Test different log levels
log("INFO: This is an info message")
log("WARNING: This is a warning message") 
log("ERROR: This is an error message")

# Test some agent operations that might generate logs
agent test_agent {
    name: "TestAgent"
    description: "A test agent for logging verification"
}

log("Agent created successfully")

# Test some calculations that might trigger logging
result = 42 * 2
log("Calculation result: " + str(result))

log("TUI logging test completed")
