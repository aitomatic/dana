{
  "version": "0.2.0",

  "inputs": [
    {
      "id": "danaArgs",
      "description": "Enter Dana arguments (e.g., 'input1=... input2=...'):",
      "default": "",
      "type": "promptString"
    }
  ],

  "configurations": [
    {
      "name": "Run Dana File",
      "type": "debugpy",
      "request": "launch",
      "module": "dana.contrib.cli",
      "args": ["${file}", "${input:danaArgs}"],
      "cwd": "${fileDirname}",
      "console": "integratedTerminal"
    },
    {
      "name" : "Debug Dana File",
      "type": "python",
      "request": "launch",
      "module": "dana.core.cli.dana",
      "args": ["${file}"],
      "console": "integratedTerminal",
      "env" : {
        "DANAPATH" : "${workspaceFolder}"
      }
    }
  ]
}
