log_level("DEBUG")

# NEED TO SUPPORT REPL PROPERLY

with use("mcp", url="http://localhost:8880/websearch") as websearch:
    answer : str = reason("Who is the CEO of Aitomatic", {"enable_ipv" : True})
print(answer)

with use("mcp", url="http://localhost:8880/sensors") as sensor:
     answer : str = reason("List me all the sensors", {"enable_ipv" : False})
     print(answer)
     answer : str = reason("Read the sensor from gas_flow_sensor_data", {"enable_ipv" : True})
     print(answer)