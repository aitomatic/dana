# Smart HVAC Demo Requirements

fastapi>=0.104.1
uvicorn[standard]>=0.24.0
websockets>=12.0
pydantic>=2.5.0
numpy>=1.24.0

# POET dependencies (would be installed from opendxa package)
# For now, we'll import directly from the local opendxa implementation