from search_agent import single_search

log_level("warn")

agent Roy
def (roy: Roy) single_search(query: str) -> any:
    return single_search(query)

query = "Wire Mesh Cable Tray"

start_time = time()
response = Roy.single_search(query)
end_time = time()

print(f"Processing Time: {end_time - start_time}s")
print(response)
