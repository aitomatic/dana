# Basic pipeline definition example
# Simple function composition without complex parsing

def add_two(n):
    return n + 2

def multiply_three(n):
    return n * 3

# Test the functions directly first
log("Testing individual functions:")
log(f"add_two(5) = {add_two(5)}")
log(f"multiply_three(7) = {multiply_three(7)}")

# The pipeline should work when the AST is properly constructed
log("Pipeline functionality ready for testing")