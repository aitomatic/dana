"""Calculate & Assess Financial Health Metrics using Dana Workflows."""


from knows.financial_analysis import AssessCapIntens, CalcQuickRatio


AMDAnnualReport = use('rag', sources=['../resources/docs/AMD_2022_10K.pdf'])


cap_intens = AssessCapIntens([AMDAnnualReport])
quick_ratio = CalcQuickRatio([AMDAnnualReport])

print(f'
CAPITAL INTENSITY
=================
{cap_intens}
')

print(f'
QUICK RATIO
===========
{quick_ratio}
')
