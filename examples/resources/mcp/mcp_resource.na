"""MCP Resource Querying."""

agent Neo

resource_weather_info = get_resource('mcp', url='http://127.0.0.1:8000/mcp')

def __main__(city='Tokyo'):
    Neo.reason(f'What is the weather in {city} like today?', resources=[resource_weather_info])
